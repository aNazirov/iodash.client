export enum RoleType {
  Admin = 1,
  User,
}

export enum SlideoverModes {
  edit = "Edit",
  show = "Show",
  create = "Create",
  none = "None",
}

export enum THead {
  avatar = "Avatar",
  edit = "Edit",
}

export enum DifficultyLevel {
  first = "first",
  second = "second",
  third = "third",
  fourth = "fourth",
  fifth = "fifth",
}

export enum Gender {
  male = "male",
  female = "female",
}
